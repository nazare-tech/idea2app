MAJOR premises Scenario cycle — Scenarios 2 and 3 omit four of their five file names (`…`), while the component is required to swap all node contents and the data test requires five files. Implementers must invent user-visible copy, so parity with the claimed source cannot be established. Fix: specify every scenario field and all rendered node copy, or commit/reference a versioned source artifact at a repository path.

MAJOR premises Assumptions / Geometry — The asserted source of truth is an imported design file named but not located, versioned, or included in the implementation inputs. Geometry and visual-content decisions can silently change or be inaccessible to the implementer. Fix: provide a repo path and immutable revision/export, or make this plan self-contained with the remaining rendering specifications.

MAJOR verification and rollback Tests / Test strategy — The plan specifies Vitest + RTL even though the documented test command uses Node’s built-in runner via `tsx`; it also omits the documented required build verification. The new test may never execute in CI, and client-component/CSS/build integration failures can ship. Fix: use the repository’s Node test-runner conventions and explicitly run `npm run lint`, typecheck, test, and `npm run build`.

MAJOR verification and rollback Runtime and Change-Impact Analysis — The proposed waitlist-branch verification “by toggling the waitlist threshold locally” conflicts with the UI-verification rule prohibiting patched/bypassed state for evidence. The changed hero container could break the waitlist form layout without valid real-flow verification. Fix: verify against a legitimately configured waitlist state, or report that branch as blocked rather than claiming coverage.

MINOR runtime and change impact D3 / Scenario cycle — Reduced motion is checked only when the effect starts. A visitor who enables `prefers-reduced-motion` while the page remains open continues receiving the JS retyping/crossfade loop. Fix: subscribe to the media-query change event and cancel/reset the loop when reduction becomes enabled.

MINOR architecture and reuse Scope / docs/systems/product-overview.md — The scope omits the system document that explicitly records landing-page hero artwork, violating the self-healing documentation rule. The canonical product documentation can continue describing the retired reel. Fix: include the relevant landing-hero documentation update in the implementation and rollback plan.
