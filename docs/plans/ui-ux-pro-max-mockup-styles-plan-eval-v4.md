BLOCKER runtime-and-change-impact "Prompt integration" / "Boundary And Cache Semantics" — the plan flips style source globally per-deployment but a project's A/B/C options are generated (and re-generated) as separate calls, so a project whose Option A/B shipped under the legacy bank will get a Pro Max Option C on retry or option-recovery. The founder's whole reason for three concepts is side-by-side comparison; they will see two concepts in one visual language and a third in an unrelated one, and the plan's "mixed old/new saved projects are supported" does not cover mixing inside one project. Fix: at `generateAndStoreOption()`, if any sibling option row for the project already exists, resolve the triad from the same source that produced it (legacy stays legacy for the remaining options); need `src/lib/mockups/` and the mockups row shape in `src/types/database.ts` to confirm siblings are readable there.

BLOCKER verification-and-rollback "Risk-Matched Verification" / Phase 4 / Milestone 4 — `MOCKUP_PROMAX_ENABLED` defaults on while the only evidence for the thing being changed (does the image provider actually honor the new treatment block) is explicitly recorded as unverified, and `docs/operating-system/ui-verification.md` forbids counting fixture/stubbed output as verification. Every new mockup run is a paid, 790s OpenRouter call, so the first real test of the prompt rewrite is customers spending credits on possibly worse output, with detection only via manual inspection. Fix: default the flag to `0`, ship dark, and make "default on" a separate one-line follow-up gated on one approved real-provider run; the user's "use Pro Max in production" does not require enabling it before any provider evidence exists.

MAJOR architecture-and-reuse "Architecture Improvement Opportunities" (Persist chosen style IDs — Deferred) / "Rollback And Recovery" — persistence is rejected as a "data-shape change outside approved scope", but the mockups table already carries per-row metadata written by `PATCH /api/mockups/[id]`; writing a treatment ID into an existing JSON metadata column needs no migration and no client payload change. As written, the accepted "residual cross-deploy mismatch" and the retry-drift risk are both paid for by policy rather than by cost. Fix: check `src/types/database.ts` for an existing `mockups.metadata` jsonb; if present, record `{promaxTreatmentId, catalogVersion}` on write and prefer it on retry, which also fixes the mixed-triad blocker above.

MAJOR runtime-and-change-impact "Failure And Recovery" vs "Prompt integration" — the plan states `MOCKUP_BRAND_DIRECTIONS_ENABLED` "is the only runtime kill switch … No second flag" and then defines a second flag `MOCKUP_PROMAX_ENABLED` with no precedence rule for the `PROMAX=1, BRAND_DIRECTIONS=0` combination. An implementer will pick one reading, and the rollback most likely to be used in an incident (disable style enrichment entirely) may leave Pro Max blocks in the prompt. Fix: delete the contradictory sentence, state `BRAND_DIRECTIONS=0` short-circuits before Pro Max resolution, and add a four-case flag-matrix prompt test.

MAJOR verification-and-rollback "Risk-Matched Verification" (Three concepts collapse visually) — the acceptance threshold is "three distinct style IDs; at least two distinct surface/type treatments", which is satisfied by three catalog rows whose rendered guidance is nearly identical, so the test passes exactly when the customer-visible failure occurs. Fix: assert divergence on the rendered block itself — distinct type family, distinct dominant palette hue family, and distinct density/effects tokens across all three, with the assertion running on the formatted prompt string, not the lookup IDs.

MAJOR runtime-and-change-impact "Risk-Matched Verification" (Selector silently falls back) / "Legacy-bank removal criterion" — the plan gates legacy-bank removal on `mockup_promax_fallback` staying "below 1% for two releases" but never says where that counter lives; `docs/systems/database-schema.md` and `product-analytics-event-taxonomy.md` describe `product_events` as an allowlisted, service-role-only, content-free stream whose executable allowlist is `src/lib/product-analytics/contracts.ts`, and adding an event there is analytics scope the plan never opens. If it is only a `logger.ts` line, the removal criterion is unmeasurable and the parallel bank never dies. Fix: decide explicitly — either add the event plus taxonomy doc entry as a named Phase 3 task, or restate the criterion in terms of something actually queryable.

MAJOR security-and-data-integrity "Prompt integration" / "Risk, Security, And Operations Critique" — `docs/systems/directories-and-key-files.md` mandates that prompts be assembled via `buildSecurePrompt()` with `sanitizeInput()` and `<user_input>` delimiters, and the plan never states which side of that boundary the treatment block sits on, even though the selector's inputs (design-plan tone/motif strings) are LLM output derived from user MVP text. If the treatment block is concatenated outside the secure builder, a crafted MVP idea that steers the design plan's motif text can reach the image prompt as trusted instruction. Fix: name `src/lib/prompts/mockups.ts` in the plan, require the treatment block be emitted through the existing secure-prompt path, and add a test that a motif string containing role markers cannot appear verbatim in the final prompt.

MAJOR premises "Retry/restart" under "Ownership, Scope, And Lifetime" — the claim that direction names/tones/motifs "are stable across option retries" holds only while the same stored design plan is reused; the plan itself notes a whole queue-attempt restart can re-plan, and with the legacy bank a re-plan still produced the same style per direction index, whereas Pro Max keys on plan content. Under the new design a queue retry silently changes the visual language of the not-yet-generated options in a way the current system does not. Fix: state this as a behavior change, and either pin the triad on the queue item's stored plan reference or accept it explicitly with a test covering re-plan-mid-queue.

MAJOR verification-and-rollback Phase 1 checklist — the license gate is already marked `[x]` and "License gate result" asserts MIT with no catalog-data exception, yet the same document's security critique still lists "Confirm upstream MIT license covers the CSV catalog files specifically" as an open action. A pre-checked gate on the one item that determines whether ~1.8 MB of third-party data may be vendored at all is self-blessing before review. Fix: uncheck it, record the exact license file path and commit hash inspected in the provenance note, and require a reviewer signature in Phase 5 before the CSVs land.

MINOR verification-and-rollback "Performance" / "Repeated Work" — a p95 < 5 ms threshold for an in-memory scan of fewer than 200 keyword rows with no I/O cannot fail even if the selector is badly wrong; the test buys nothing and adds 1,000-iteration flake surface in CI. Fix: drop it and keep only the 100 KB lookup-size assertion and the existing `guard-webpack-chunky.mjs` build guard, which are the constraints that can actually be violated.

MINOR product-and-scope "Implementation Phases" Phase 4 — `docs/operating-system/planning-workflow.md` requires backend/server-side changes to append to `docs/plans/backend-change-history.md`, and this plan is explicitly a user-approved server-side prompt change, but the doc list names only mockup/setup/key-files/scripts/test-inventory. The approval and its rollback notes end up unrecorded in the place the next agent will grep. Fix: add the backend-change-history append, including the 2026-08-09 approval and both flag names, to Phase 4.

MINOR runtime-and-change-impact "Source snapshot" / Phase 2 — the 100 KB budget is stated for the generated lookup but the plan never says the vendored CSVs, frozen raw fixtures, and generator script are excluded from the Next.js server bundle for `/api/mockups/generate` and `/api/generate-all/execute`. A stray import or a glob in the skill directory pulls megabytes of CSV into the serverless function and trips the build guard late, in Phase 3. Fix: place vendored data and generator outside `src/`, and assert in the Phase 2 build guard that only the generated TS module is reachable from the route graph.
