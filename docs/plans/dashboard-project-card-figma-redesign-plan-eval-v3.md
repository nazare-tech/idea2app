MAJOR premises `## Clarifying Questions` Q1 — the plan selects Recommendation B while `docs/operating-system/planning-workflow.md` mandates Recommendation A unless the user's prompt, a hard safety constraint, or `recommendation-selection-rules.md` says otherwise, and the justification given ("the user's requested source of truth") is an inference from a Figma node reference, not a recorded user instruction. If built, `/projects` on a 1440px+ viewport shows 420px cards with a large ragged dead zone, a page-level layout change beyond the named component, and no generalized rule is captured for the deviation. Fix: either quote the user instruction that overrides the default and append the derived rule to `docs/plans/recommendation-selection-rules.md`, or revert to A (fluid grid, Figma internals at 420px reference).

MAJOR architecture-and-reuse `## Runtime and Change-Impact Analysis > Ownership` — the plan assumes `DashboardProjectCard` has exactly one caller (`/projects`) but never verifies it; hard-coding `w-[420px] h-[430px]` into the component itself breaks any other consumer. If a recent-projects section or workspace picker also renders this card, those surfaces silently get 420×430 tiles inside a differently-sized container. Fix: grep for `DashboardProjectCard` imports before implementing; if there is more than one caller, put the width on the `/projects` list item and keep the component fluid.

MAJOR verification `## Test Strategy` / `Risk-Matched Verification` — the sole geometry gate is one-time manual "browser bounding-box inspection," and the plan explicitly disclaims rendered-size coverage from markup tests, so nothing in CI fails when the 430px/188px/420px contract is broken later. The next Tailwind or typography change silently reflows every card with no failing test. Fix: add a `@playwright/test` Chromium assertion (the repo already has it) on `boundingBox()` for card and panel at desktop and narrow widths, run in the normal suite.

MAJOR premises `## Goal` / `## Plan` step 2 — "tighter typography" and a four-line clamp inside a fixed 188px panel are specified with no concrete font sizes, line-heights, gaps, or date-block height, yet the ±2px acceptance threshold depends entirely on that math. The implementer will guess a type scale, then discover the fourth description line or the date overflows or leaves a gap, and rework both files. Fix: state the exact px values for title size/line-height, description size/line-height ×4, date size, panel padding, and inter-element gaps, and show they sum to 188.

MAJOR runtime-and-change-impact `## Runtime and Change-Impact Analysis > Failure And Recovery` — the plan replaces the earlier rem-relative min-height with literal px heights and never addresses user-agent font scaling or browser zoom with fixed root size. A user with a 20px default font size gets a clipped title and a description overrunning the 188px panel, with no reflow escape hatch. Fix: keep px widths but use `min-h` on the panel with the clamp, or express heights in rem so the card grows with the root font size; verify at 125% text size as part of the browser gate.

MAJOR product-and-scope `## Test Strategy` — the evidence requirement ("desktop with at least six cards visible in the page data") is not reconciled with `project-allowance.ts` plan gating or with `docs/operating-system/ui-verification.md`, which forbids creating fresh projects without explicit opt-in because it spends real credits. If the verification account holds fewer than six projects, the step is unachievable and the plan's final gate stalls. Fix: confirm the account's existing project count first, and if it is under six, either lower the evidence bar or get explicit opt-in for project creation before starting.

MINOR runtime-and-change-impact `## Plan` step 3 — switching `/projects` from grid tracks to wrapping flex changes last-row alignment, gutter behavior, and the sm→md transition, but no `gap`, `justify-content`, or per-breakpoint column expectation is specified. At 640–860px the layout yields a single 420px card left-aligned in a 640px column, which reads as broken. Fix: specify gap and justification, and name the expected card-per-row counts at sm/md/lg/xl.

MINOR premises `## Plan` step 1 — the 216px media cap is presented without derivation, and the plan never states whether the 430px height is border-box or content-box or how the "stronger neutral outline" border width is accounted for. With a ±2px tolerance, a 1px border on each side under content-box sizing fails the plan's own acceptance threshold. Fix: state `box-sizing` assumption and write the arithmetic: 430 − 188 (panel) − border/padding = media region, and show 216 falls out of it.

MINOR security-and-data-integrity `## Architecture Improvement Opportunities` item 2 — hard-coding `#c9c9c9` and `#f6f6f6` is justified by the claim that `/projects` is always the light `bg-background` surface, but the coding-conventions header documents `sidebar-*` dark tokens, so a theme mechanism may exist. If a dark surface is ever applied to the dashboard main area, the description panel becomes an unreadable light block with no token to fix it centrally. Fix: check `src/app/globals.css` for a `.dark` block or theme provider; if one exists, add the two values as scoped CSS variables rather than inline hex.

MINOR verification `## Rollback Or Recovery` — the rollback list names only "card/thumbnail layout classes and related docs" but the plan also rewrites the `/projects` page container from grid to flex. Reverting the two components without the page file leaves fluid cards inside a wrapping flex container, a layout that matches neither design. Fix: name the `/projects` page file explicitly in the rollback set.

MINOR product-and-scope `## Test Strategy` — the evidence path hard-codes `ui-evidence/2026-08-09/`, which is only correct if implementation lands the same day the plan was written. Evidence gets filed under a date that does not match the work, breaking the `ui-evidence/<date>/<task-slug>/` convention. Fix: write the path as `ui-evidence/<implementation-date>/project-card-figma-redesign/`.

MINOR runtime-and-change-impact `## Runtime and Change-Impact Analysis > Failure And Recovery` — the fixed 430px card with a fixed 188px panel does not define bottom-anchoring for the italic date when the description is short or the fallback copy is used. Cards with one-line descriptions will show the date floating mid-panel while neighboring cards show it near the bottom, producing a visibly uneven row. Fix: specify that the date is pushed to the panel bottom (`mt-auto` on a column flex panel) and include a short-description card in the browser evidence.
