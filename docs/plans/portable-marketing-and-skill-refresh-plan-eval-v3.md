MAJOR premises Boundary And Cache Semantics / step 6 — the `4k-vfx` removal gate only asserts that the surviving `vfx` route emits `resolution: 4k` on `standard` with a cost gate; it never diffs against `4k-vfx`'s own current contract (its model tier, upscaling steps, or extra parameters). If `4k-vfx` did anything `vfx` does not, 4K rendering silently regresses after deletion even though every assertion passes. Fix: make the removal gate a per-field crosswalk of `4k-vfx`'s actual instruction contract against `vfx`, not a standalone parse of `vfx`.

MAJOR product Assumptions ("Marketing captures belong to the current repository") / step 8 — capture writes `marketing/` into whatever Git root is cwd, and the only guarded case is *no* Git root; running the portable skill while working inside an unrelated code repo dumps private marketing strategy into that project's tree. Consequence: unpublished strategy gets scattered into (and possibly committed to) arbitrary repos. Fix: require explicit confirmation that the current repo is the intended marketing target, or designate a marketing repo, before the first write.

MAJOR verification Validation ("No paid render is fired") — 4K correctness is verified only by statically parsing the instruction file; there is no real-flow evidence that `vfx --resolution 4k` actually renders after `4k-vfx` is gone. A route that parses correctly but breaks at generation time passes the gate. Fix: add an opt-in live 4K probe (behind explicit spend authorization) as the post-removal acceptance check, keeping the static parse only as the pre-removal blocker.

MINOR security Failure And Recovery ("Existing marketing filename") — the branch between "distinct idea" (SHA token) and "true repeat/revision" (`-v2`) depends on the skill correctly judging semantic sameness of two Markdown bodies, which it cannot do reliably. Consequence: revisions get SHA-suffixed as new ideas (or distinct ideas get versioned onto each other), producing misfiled/duplicated captures. Fix: make the rule deterministic — key on slug/date only and always allocate `-vN`, dropping the content-semantics distinction.

MINOR verification Validation ("`quick_validate.py`") — the plan's core skill-validation step and Risk-Matched Verification depend on `quick_validate.py` and a "skill validator"/symlink checker whose existence I cannot confirm from the supplied docs (setup-and-build lists only `npm run lint/test/build` and tsx). If those tools do not exist, the validation phase has no executable backing. Fix: name and confirm the validator path (`quick_validate.py`, skill schema checker) exists before relying on it as the acceptance gate.
