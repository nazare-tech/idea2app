MAJOR premises docs/plans/portable-marketing-and-skill-refresh-plan.md (Assumptions / Plan step 4) — the plan removes `4k-vfx` on the premise that general `vfx` "owns ... 4K" via `vfx --resolution 4k`, but nothing in the plan verifies that flag exists today or is added first (step 3 refreshes Pika `vfx` from upstream, not the general `vfx`). If built as written, 4K rendering is stranded the moment `4k-vfx` is trashed. Fix: gate step 4 removal on a confirmed, tested 4K path in the surviving `vfx` skill; I'd need to see skills/vfx/SKILL.md and skills/4k-vfx to confirm.

MAJOR verification docs/plans/portable-marketing-and-skill-refresh-plan.md (Risk-Matched Verification / Validation) — every acceptance check is structural (recursive diff, `rg` for dangling refs, `quick_validate.py`, symlink resolution) and none exercises a replacement's *behavior*; a validator-passing `vfx` that silently no-ops on 4K, or a ported `ci-operator` that doesn't run CI, sails through. If built as planned the tests pass while capability is lost. Fix: add a functional smoke of each replacement path (invoke 4K render, run the ported CI/UI operator) as an acceptance gate.

MAJOR runtime docs/plans/portable-marketing-and-skill-refresh-plan.md (Plan step 7 / Rollback) — step 7 edits the starter kit's existing router and README, but the plan states the starter kit is untracked and the rollback only covers "remove newly added folders." Corrupting or wrongly rewriting the existing untracked router/README leaves no recovery. Fix: snapshot the starter kit (copy/tar) before editing, and document restore-from-snapshot in Rollback.

MAJOR architecture docs/plans/portable-marketing-and-skill-refresh-plan.md (Plan step 4 / Boundary semantics) — removing global `ci-watcher`, `fix-ci`, `loop-on-ci`, `run-smoke-tests`, `control-ui` is justified by "existing CI/UI project routers replace deleted global leaves," but the CI/UI operators are only *ported into the starter kit* (step 7), i.e. for new projects; the plan never establishes that the current repo has an equivalent project-level router. If it doesn't, the active repo loses its CI-fix/UI-verify workflows entirely. Fix: confirm (name the file) a current-repo router covering these before deletion; I'd need root AGENTS.md/CLAUDE.md and any project skills/ dir to judge.

MINOR premises docs/plans/portable-marketing-and-skill-refresh-plan.md (Ownership / Failure And Recovery) — the durability claim ("captures ... versioned through normal Git policy") collapses on the documented "no Git repo → use active workspace root" fallback; in a throwaway workspace (this session's temp dir) captures are unversioned and lost on cleanup, defeating the goal. Fix: on the no-Git path, warn the user the capture is unversioned and offer an explicit persistent target rather than silently writing to a temp root.

MINOR security docs/plans/portable-marketing-and-skill-refresh-plan.md (Clarifying Q3 / Failure And Recovery) — the "timestamped Trash folder" location is unspecified; if placed inside the repo/workspace it re-enters skill discovery or gets committed, undoing the removal and leaking retired skills into Git. Fix: pin Trash to a path provably outside Codex discovery and outside the repo, and assert that in the deletion-results validation.
