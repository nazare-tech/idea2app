BLOCKER runtime-and-change-impact "Prompt integration" / "Failure And Recovery" — the plan states both "Kill switches: disable Pro Max only, or disable all brand directions through the existing flag" and "`MOCKUP_BRAND_DIRECTIONS_ENABLED=0` … the only runtime kill switch"; no Pro Max-only switch exists, and that flag restores the *pre-bank* prompt, not the current bank. If Pro Max prompts degrade in production, the only same-day lever throws away the known-good hand-authored bank too, and restoring today's behavior needs a code revert and deploy. Fix: either add a `MOCKUP_PROMAX_ENABLED` flag (default off at first deploy, flipped on after real-provider evidence) or delete the false claim and state plainly that Pro Max rollback is code-revert-only.

BLOCKER verification-and-rollback "Phase 4" / "Test Strategy" / docs/operating-system/ui-verification.md — the sole customer-visible effect of this change is how OpenRouter's image model renders the new treatment block, and every listed verification (prompt snapshots, fixture-mode workspace, Codex built-in image generation) deliberately excludes that provider; the repo's UI-verification rule forbids fixtures/stubbed providers as evidence and requires reporting the blocker instead. As written, Milestone 4 ("No-OpenRouter visual QA … pass") can be checked off and the plan marked implemented while the change ships default-on, entirely unverified against the model it targets. Fix: treat the no-OpenRouter constraint as a blocking gate — ship behind a default-off Pro Max flag, and ask the user for explicit opt-in to one real paid generation run (the fresh-project evidence rule is opt-in, not prohibited) before enabling.

MAJOR premises "Runtime selector" / "Ownership, Scope, And Lifetime" — the determinism claim rests on the unverified assumption that single-option regeneration re-reads a *persisted* `MockupDesignPlan` rather than re-running the planner model; if any retry path re-plans, direction names/tones/motifs change and the treatment silently drifts while the determinism unit test ("byte-identical IDs for identical inputs") still passes. Fix: confirm in `src/lib/prompts/mockups.ts` and the `/api/mockups/generate` + manual-option handler that the retry path loads the stored plan; if it does not, that is a prerequisite phase, not an assumption.

MAJOR runtime-and-change-impact "Failure And Recovery" — "never mix Pro Max and legacy treatments within one selector result" only constrains a single call, but selection runs again on single-option retry and on `generation_queue_items` attempt retries; a catalog/deploy change or a fallback that fires on the second call yields a project whose A/C are Pro Max and B is legacy bank, which the user paid for and cannot detect or fix. Fix: derive the fallback decision and treatment IDs once per run and pass them through the existing in-memory run context to all option calls, and define the accepted behavior for cross-deploy queue retries explicitly.

MAJOR verification-and-rollback "Risk-Matched Verification" / "Failure And Recovery" — the fallback path is the plan's safety valve but has no acceptance threshold and no observability beyond a content-free log line; if token matching misses on real-world design plans (short, non-English, or unusual product framing), every generation silently falls back to the old bank and the feature ships as a no-op that nobody notices. Fix: add a measured fallback-rate threshold over real historical design plans (not just the ten curated ideas) plus a counted, queryable diagnostic so the rate is visible post-deploy.

MAJOR security-and-data-integrity "Runtime selector" / "Risk, Security, And Operations Critique" — allowlisting, normalization, and length caps are all described as happening at *generation* time in the offline script; the checked-in runtime lookup itself has no validating test, and "malformed generated catalog → build/unit tests fail" is asserted without saying what test would fail. A bad regeneration pastes catalog prose (including anything instruction-shaped) straight into a paid image prompt. Fix: add a CI unit test over the committed lookup enforcing field enums, max lengths, and a character/instruction-pattern denylist, independent of the generator that produced it.

MAJOR architecture-and-reuse "Phase 1" / "Clarifying Questions Q4" — the MIT-covers-the-CSVs check is a checkbox *inside* the same phase that vendors the CSVs, and Recommendation A is already selected on the assumption it passes; if the catalog data turns out to be separately licensed, the vendored snapshot must be ripped back out and the plan has no alternative design. Fix: make the license finding a gate before any vendoring, and name the fallback path now (derive a keyword/category mapping and cite upstream without redistributing the CSV corpus).

MAJOR verification-and-rollback "Risk-Matched Verification" (first row) — "frozen raw outputs match generator inputs" is a hash comparison against files committed by the same change, so a hand-edited or partially-run fixture is indistinguishable from genuine upstream output; the anti-drift control cannot detect the exact failure the Engineering Critique names ("claiming Pro Max while implementing a loose reinterpretation"). Fix: commit the exact query set, upstream script hash, and a one-command reproduce script, and require an independent re-run of that command as the acceptance evidence.

MAJOR premises "Prompt integration" / "Test Strategy" — Prompt Lab drafts persist with `project_id NULL` and are local-dev only, so a Prompt Lab invocation may have no real persisted design plan; the selector would then take the fallback branch, and the "Prompt Lab matches production prompt building" integration test would pass vacuously on the legacy path while proving nothing about Pro Max. Fix: verify what design-plan object the `/api/dev/prompt-lab/run` and `mockup-image` routes supply, and assert in the test that the Pro Max branch (not fallback) was exercised.

MAJOR runtime-and-change-impact "Rollback And Recovery" — Generate All runs mockups as a dependent queue step with a 790s/800s provider timeout and sequential Options A/B/C, so a deploy mid-run is not hypothetical; "avoid active generation windows where practical" is not a mechanism and the plan otherwise treats this as pre-existing risk, which it is not — today's bank is a static in-repo constant that changes only with the same deploy that changes the prompt. Fix: state the deploy policy concretely (catalog changes are release-gated behind the Pro Max flag flip, not a bare code deploy) or accept persisting the triad IDs on the queue item.

MINOR verification-and-rollback "Runtime and Change-Impact Analysis" vs "Test Strategy" — the selector performance acceptance threshold is p95 < 5 ms in one section and p95 < 25 ms in another over the same 1,000 iterations; whoever implements will pick the loose one and the tight number becomes decorative. Fix: state one threshold.

MINOR architecture-and-reuse "Prompt integration" / Architecture Opportunities — the hand-authored 15-kit bank is retained indefinitely as whole-triad fallback with no removal criterion, leaving two parallel style systems that both need updating whenever prompt formatting changes. Fix: define the condition and follow-up task under which the legacy bank is deleted (e.g. fallback rate below N% for one release), or explicitly accept it as permanent and say so.

MINOR verification-and-rollback "Risk-Matched Verification" (triad collapse row) — "300 synthetic IDs" is undefined; if the synthetic inputs are generated from the same allowlisted keyword vocabulary the selector matches on, the diversity test is tautological and will pass regardless of the selector's real behavior. Fix: specify the synthetic-input source and include adversarial cases (empty/short plans, off-catalog domains, near-duplicate directions).

MINOR runtime-and-change-impact "Phase 3" — the 1,600/1,800-character caps are asserted with no recorded baseline for today's brand-kit block or the image model's effective prompt budget, so the cap could be either a no-op or a silent truncation of guidance. Fix: measure the current block and total prompt size in Phase 2 and set the caps relative to that measurement.

MINOR product-and-scope "Rollback And Recovery" — "revert the prompt-selector commit" assumes the phases land as separable commits, but Phases 1–3 are described as one implementation stream (vendored data, generator, selector, prompt swap). Fix: require the prompt-swap to be its own commit, isolated from the vendoring and selector commits.
