BLOCKER runtime-and-change-impact "Plan" step 4 / "Ownership, Scope, And Lifetime" — Opening a Radix Dialog from a Radix DropdownMenu item is the known `pointer-events: none` trap: the menu's dismissable layer unmounts while the dialog mounts, and body pointer-events are left disabled, or the dialog is unmounted with the menu subtree. Built as written, selecting `Rename` yields a dashboard where the modal appears but the whole page (or the page after Cancel) is unclickable, and this is exactly the state the planned "Rename opens dialog; Escape/Cancel closes" assertion will pass through since keyboard still works. Fix: hoist the Dialog out of the `DropdownMenuContent` subtree, drive `open` from card state set in `onSelect` with `event.preventDefault()`, and add a Playwright assertion that a second card's link is clickable immediately after Cancel.

MAJOR architecture-and-reuse "Architecture Improvement Opportunities" bullet 3 / Assumptions — The plan imports the 80-code-unit cap from `project-name-generation.ts` into client card code, but that module is an AI-naming module likely pulling prompts/model-policy/server-only deps. Built as written this either bloats the dashboard client bundle with generation code or fails the build with a server-only import in a `"use client"` component. Fix: extract `PROJECT_NAME_MAX_LENGTH` and the pure normalizer into a dependency-free module (e.g. `src/lib/project-name.ts`) and have `project-name-generation.ts` import from it, not the reverse. I would need `src/lib/project-name-generation.ts` and `src/lib/project-update.ts` to confirm the current import graph.

MAJOR premises "Boundary And Cache Semantics" / "Assumptions" — The card recomputes a UUID-plus-slug href locally after rename, but the plan never names the slug function or states it is a pure client-importable helper shared with the server that generates canonical refs. If the client re-derives the slug with different rules (NFKC output, stripped punctuation, case, length truncation) than the route's canonicalizer, every renamed card links to a non-canonical ref and the redirect the optimization was meant to remove happens on every click, silently. Fix: name the single exported slug helper, import it in the card, and add a unit test asserting `slugify(normalize(name))` equals the value the workspace canonicalizer would redirect to; I need the file implementing `projectRef`/slug parsing to verify it is client-safe.

MAJOR premises "Boundary And Cache Semantics" / Clarifying Question 2 — Client validates 1-80 characters on the raw draft while the server caps 80 UTF-16 code units *after* NFKC normalization, and NFKC expands characters (ﬁ→fi, ½→1⁄2, ㍿→株式会社). A user pasting an 80-character name containing any compatibility character passes client validation, gets a 400, and sees only the planned "generic inline message" with no way to understand or fix it. Fix: run the identical shared normalizer on the client before the length check and show the normalized value in the field so client and server agree on the same string.

MAJOR runtime-and-change-impact "Failure And Recovery" / "Boundary And Cache Semantics" — The plan sets local display name from the client draft, but the server persists the normalized string (whitespace-collapsed, NFKC, controls stripped). Typing `My  App ` shows `My  App` on the card while `My App` is stored, and the discrepancy only resolves whenever `router.refresh()` lands — which the plan also treats as the reconciliation path, so the title visibly changes after the modal closes. Fix: set display name and href from the `{ data }` response body's `name`, not from the draft.

MAJOR verification-and-rollback "Test Strategy" / "Risk-Matched Verification" — The Playwright journey mutates a retained real project in the shared UI-test account and relies on a `finally` PATCH to restore. If the run is interrupted, the token has expired, or two runs overlap, the 80-character marker becomes the account's persisted project name and poisons the very geometry/description assertions the same suite depends on. Fix: capture the original name before mutation, gate the whole journey on a designated fixture project id (skip loudly otherwise), and make restore-failure a hard test failure with a retry rather than best-effort cleanup.

MAJOR verification-and-rollback "Plan" step 2 / "Risk-Matched Verification" — The verification table has no evidence that the hover-only delete control is actually gone or that the new trigger is visible without hover; the "Delete regression" row only exercises Delete *through* the overflow menu. Built as written, a leftover hover trash button on the media survives every listed check, so the stated goal of the change is untested. Fix: add an assertion that no delete control exists in the thumbnail region, and that the overflow trigger is visible with zero pointer interaction (and under `hover: none` emulation).

MAJOR security-and-data-integrity "Plan" step 5 / Assumptions — "Repository search found one PATCH caller" is a single-source assumption used to justify tightening a shared write boundary; the API doc header lists `/api/projects/[id]` GET/PATCH alongside composer, intake-create, generate-all, and onboarding-status flows that also write `projects.name`. If any other client (workspace header rename with its own max length, or an internal caller) sends a name the new validator rejects, that flow starts 400-ing with an error shape it never handled. Fix: enumerate callers explicitly in the plan and update the workspace header rename client to the shared validator and error rendering; I need `src/app/api/projects/[id]/route.ts` and the workspace header rename component to confirm.

MINOR security-and-data-integrity "Boundary And Cache Semantics" — Normalization is enforced only at this one route; project names are also written by intake creation and AI naming, so the 80-code-unit and control-character invariant is not a data invariant. Existing or future rows can hold names that violate it, and the card/slug path must tolerate them. Fix: state that the card and slug helper render defensively for legacy names, and route the other name writers through the same normalizer (or note explicitly that they are out of scope).

MINOR architecture-and-reuse "Boundary And Cache Semantics" — Nothing states the recomputed href is URL-encoded. A name normalizing to non-ASCII produces a slug segment that, if interpolated raw into `href`, yields a malformed or double-encoded URL after Next's own encoding. Fix: specify ASCII-only slug output (transliterate/strip) or explicit `encodeURIComponent` on the slug segment, with a unit case for a non-Latin name.

MINOR runtime-and-change-impact "Ownership, Scope, And Lifetime" — "Draft/display state resets on a changed server prop" has no stated mechanism; a `useState` initialized from props will not resync when `router.refresh()` returns a name changed elsewhere (workspace rename in another tab), leaving the card permanently stale. Fix: specify the sync mechanism (derive display from the prop and keep only an override that clears when the prop changes, or key the card on `id + name`).

MINOR product-and-scope "Risk-Matched Verification" / "Test Strategy" — The trigger's accessible name is never specified, so a dashboard of N cards renders N identically-named "More" buttons and the planned "Radix roles" check will pass anyway. Fix: require an accessible name that includes the project name and assert it in the browser suite.
