BLOCKER security "Plan" step 2 / Architecture "Secure no-shell Codex invocation" — an authenticated API route that spawns the operator's local Codex CLI and drives a real Chrome profile makes any signed-in user's request execute under the server operator's local OS session and logged-in social cookies; the env flag `RESEARCH_CODEX_POSTING_ENABLED` gates the feature globally but grants no per-user authorization. If built as written, user B triggers a post that publishes from the operator's (or user A's) social account, and no ownership check exists to prevent it. Fix: restrict both routes to a dev/self-hosted single-operator mode (assert `NODE_ENV !== 'production'` plus an operator allowlist of user IDs) and state explicitly that this path never ships to the hosted Vercel deployment.

BLOCKER premises Architecture "Deferred: Live Last30Days JSON ingestion" vs "Plan" step 3 "general/adaptive find-more" — the discovery adapter is deferred to a later worker integration while the UI ships a "find more" action; with an immutable committed seed there is no additional corpus to return. The primary differentiating interaction ships as a button that can only re-sort items the user has already seen, and QA in step 5 ("broad/adaptive find-more") will pass while delivering nothing. Fix: either scope find-more to "expand page size over the seed" and label it that way, or move the Last30Days JSON file-ingestion adapter into this increment — do not ship an unbacked discovery affordance.

BLOCKER security "Runtime And Change-Impact Analysis → Ownership" ("`ResearchInboxState` belongs to one workspace slug in versioned browser storage") — state is keyed by workspace slug, not by authenticated user ID, and nothing clears it at sign-out. On a shared browser, user B signing in sees user A's saved items, archived judgments, and content-bearing generated drafts; this survives logout indefinitely. Fix: key storage by `user.id` (or a hash of it) plus slug, and clear/ignore foreign-keyed namespaces on auth state change.

MAJOR runtime-impact "Plan" step 2 "authenticated Codex draft API" — the repo already meters every AI generation through `getTokenCost()`/`consume_credits` (docs/systems/credits-and-billing.md), and this plan adds an authenticated generation endpoint with no credit consumption, no refund path on failure, and no reference to `src/lib/rate-limit.ts`. Any signed-in user gets unmetered model calls, and the credits ledger silently stops describing all AI spend. Fix: decide and state explicitly — either charge via `getTokenCost()` with refund-on-failure symmetry, or document the route as zero-cost local-only and enforce that with the operator gate above. I would need `src/lib/token-economics.ts` and `src/lib/rate-limit.ts` to confirm the exact integration points.

MAJOR architecture Architecture "Provider ports for discovery, reply generation, and posting" / Risks "Nested Codex runtime" — reply generation is routed to a local CLI while the repository already has a server-side generation stack (OpenRouter via `analysis-pipelines.ts`, plan-tier routing in `generation-model-policy.ts`, `buildSecurePrompt()`/`sanitizeInput()` in `src/lib/prompts/`). Building a second, CLI-shaped generation path means the "sellable" core cannot run on Vercel at all and duplicates prompt-security machinery that already exists. Fix: implement the reply port against the existing OpenRouter path as the default adapter and keep Codex CLI as an optional local adapter behind the same port.

MAJOR security "Runtime And Change-Impact Analysis → Failure And Recovery" ("Post failure never marks replied. Unknown outcomes require manual reconciliation") — the enumerated state model is seen/saved/archived/replied with no `post_attempted`/`unknown` state, and the idempotency record ("one confirmed draft hash per attempt") lives in browser localStorage. After a timeout mid-post, a reload leaves the item looking un-replied with no attempt record, and the user re-posts a duplicate to a public network. Fix: add an explicit `post_attempted_at` + `unknown_outcome` state written *before* the connector call, persist the draft hash with it, and require the user to resolve unknown outcomes before the post button re-enables.

MAJOR verification "Validation" ("No public external post during verification") + Risk table "Duplicate posting → Confirmation/idempotency contract" — the single highest-risk path gets no real-flow execution and no described dry-run harness; a unit test of a confirmation contract passes whether or not the connector actually posts, double-posts, or reports success on failure. The feature ships with its dangerous branch never having run. Fix: build an explicit `dry-run` posting connector that exercises the full confirm→invoke→outcome→state-write path against a local sink, and require its evidence under `ui-evidence/` in step 5.

MAJOR product-scope "Goal" ("Seed it with current Maker Compass problem research") + Boundary ("Seed data is immutable source evidence") — committing verbatim third-party social comments and their provenance into the repo conflicts with docs/operating-system/transcript-sanitization-protocol.md and research-format.md, which permit only sanitized derived outputs. Building this bakes external users' raw text and identifiers into git history where removal requires a rewrite. Fix: state the seed's sanitization treatment explicitly (excerpt limits, no author handles/URLs to personal accounts) or load the seed from a gitignored local artifact and ship only the schema.

MAJOR verification Risk table "Bad adaptive ranking → Replied outranks saved; archived demotes" — this threshold tests the status ordering, which is trivially true by construction, not the exemplar-transfer logic that is the actual claim ("feedback must visibly improve curation"). A ranking whose feature weighting is entirely broken passes every listed test. Fix: assert that an *unlabeled* item sharing features with replied exemplars outranks an unlabeled dissimilar item, and that removing all feedback restores the neutral order.

MAJOR process "Rollback Or Recovery" / "Plan" step 4 — docs/operating-system/planning-workflow.md requires persistence and data-shape changes to append to `docs/plans/backend-change-history.md`; this plan introduces a versioned persistence format, two API routes, and a new env var without naming that file, and step 4 says only "add documentation". The plan will be marked implemented with a required durable record missing and no schema-version migration note for future readers. Fix: add the backend-change-history.md append plus the specific docs/systems/ files to update (api-endpoints.md for the two routes, setup-and-build.md for `RESEARCH_CODEX_POSTING_ENABLED`) to step 4.

MINOR product "Plan" step 3 "browser selector" + Rollback "Disable automated posting globally by leaving `RESEARCH_CODEX_POSTING_ENABLED` unset" — the default state is disabled, so every user sees a browser-mode selector wired to a capability that does not exist for them. The primary next action after generating a draft is ambiguous dead UI. Fix: hide the selector entirely when the server reports posting disabled and make copy/open the single primary action in that mode.

MINOR runtime-impact "Repeated Work" ("recomputes only when source, status, query, sort, or feedback state changes") — feedback state changes on every save/archive click, so the whole list re-ranks and reorders under the user's cursor mid-triage. Rows jump between clicks, causing mis-archives, which directly contradicts the end-user critique about triage ergonomics. Fix: freeze list order for the duration of a triage session; apply newly learned ranking only on explicit refresh or find-more.

MINOR architecture Architecture "Provider ports for discovery, reply generation, and posting" — three port abstractions ship with exactly one implementation each, and the discovery port's second implementation is explicitly deferred. That is indirection with one caller, and it makes the pure-logic tests in step 1 test mocks rather than behavior. Fix: keep the repository/persistence seam (it has a named next adapter) and inline discovery and posting until a second implementation is actually being written.

MINOR product-scope "Goal" ("sellable research workbench") + step 4 "Wire `/research` into authenticated shell" — no entitlement or plan-tier gating is specified, so the route appears for Free-tier users alongside `project-allowance.ts`-gated features. Either it is silently free forever or gating gets retrofitted after users have state in it. Fix: state the tier decision now and name the guard used.
