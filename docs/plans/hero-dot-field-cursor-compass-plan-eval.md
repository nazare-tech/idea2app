MAJOR Architecture and reuse Plan step 2 — “wedge path from `compass-mark.tsx` geometry” assumes a React/SVG component’s geometry is directly reusable by Canvas, but no shared geometry contract or extraction is planned. This will likely produce duplicated path data that drifts from the brand mark. Fix: extract neutral path/point constants from `compass-mark.tsx` into a shared module consumed by both renderers.

MAJOR Runtime and change impact Plan steps 1–3 — The protected headline ellipse and build-map fade require the actual responsive bounds of the copy/CTA and `HeroBuildMap`, yet the plan defines neither DOM measurement nor a layout contract. At intermediate widths, wrapped copy or a relocated map can be covered by visible dots. Fix: measure named protected/map elements on resize (with padding), or define the zones in the hero layout itself; inspect `src/app/page.tsx` and `HeroBuildMap` to establish the contract.

MAJOR Runtime and change impact Runtime and Change-Impact Analysis — Pause/resume is described as separate IntersectionObserver and visibility handlers, but no combined lifecycle state is specified or tested. A hidden tab can resume the rAF while the hero remains offscreen, or an intersecting hero can remain frozen after visibility returns. Fix: use one `shouldAnimate = isIntersecting && !document.hidden && !reducedMotion` gate and test each transition sequence.

MAJOR Product and scope Assumptions / Plan step 2 — Disabling interaction based on `pointer: coarse` incorrectly disables cursor response on hybrid devices that have both touch and a mouse/trackpad. Those users receive the noninteractive mobile variant despite using a cursor. Fix: determine interaction from actual mouse/pen pointer events or `(any-hover: hover)`, while retaining north-facing wedges only where hover is unavailable.

MINOR Verification and rollback Validation — `npm test (or repo runner)` is not an executable verification instruction, despite the system header naming `npm run test` and CI’s required checks. The core tests may be omitted or run under a different command without anyone noticing. Fix: specify `npm run test`, `npm run lint`, typecheck, and build as required commands.

MINOR Architecture and reuse Plan / Rollback Or Recovery — The plan changes landing-page behavior but omits the required same-change system documentation update. The landing hero/component documentation will become stale immediately. Fix: identify and update the relevant landing-artwork entries in `docs/systems/product-overview.md` and `docs/systems/directories-and-key-files.md`.
