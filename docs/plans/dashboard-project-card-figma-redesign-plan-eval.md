MAJOR premises "Goal" / "Failure And Recovery" — the 430px card / 188px panel / 216px media numbers are derived at the 420px Figma reference width, but the plan keeps a responsive grid where cards render narrower; a description clamp tuned for 420px-wide lines wraps to more lines at ~340px, and a fixed 188px panel cannot absorb them. Built as written, narrow-viewport cards clip descriptions mid-word or overflow the panel border while the browser check ("approximately 430px tall") still passes. Fix: make the lower panel `min-h-[188px]` with an explicit `line-clamp-N` and assert the clamp holds at both the 420px and the narrowest grid width in the browser evidence step.

MAJOR runtime-and-change-impact "Boundary And Cache Semantics" / plan checklist — the plan claims only the card and thumbnail change, but never mentions the `/projects` loading skeleton / placeholder card that is sized to the old card height. If the skeleton keeps the old dimensions, every dashboard load shifts the grid by the height delta when real cards mount (CLS on the primary logged-in page). Fix: add an explicit step to update the projects-grid skeleton to the same 430px contract; I would need `src/app/(dashboard)/projects/page.tsx` and any `loading.tsx` / skeleton component under `src/components/dashboard/` to confirm which file owns it.

MAJOR product-and-scope "Clarifying Questions #2" / "Risk-Matched Verification" — moving the delete control to sit "over the preview" changes it from whatever containment it has today to an overlay inside the card's navigable surface, and the plan verifies this only by "delete affordance inspection." If the delete button is nested inside the card's `<a>`, clicking delete also navigates to the workspace (or React hydration warns on invalid nesting), and keyboard focus order/visible focus ring over the image is untested. Fix: keep the delete control outside the anchor (sibling in a relatively-positioned wrapper), and add a component test asserting the delete trigger is not a descendant of the card link; needs `src/components/dashboard/dashboard-project-card.tsx` to confirm current nesting.

MAJOR product-and-scope "Failure And Recovery" — the plan enumerates missing/failed *image* states but never defines the panel's state when `projects.description` is null or empty, which is a normal row shape for projects that skipped intake description. Built as written, those cards render a 188px blank gray rectangle with only a date. Fix: specify empty-description copy or collapse behavior, and cover it in the focused component tests alongside the empty/unavailable image cases.

MAJOR verification "Test Strategy" / "Risk-Matched Verification" — the only automated additions are markup/class assertions on `ProjectCardThumbnail`, while the 430px total height and 188px panel live in `DashboardProjectCard`; nothing in the suite fails if the two fixed heights stop summing. jsdom-style markup tests also pass on a class string that produces the wrong rendered box. Fix: add card-level assertions for the height/panel classes in the same change, and state the height acceptance as a measured tolerance (e.g. 430px ±2px) rather than "approximately."

MAJOR security-and-data-integrity — not applicable as a trust-boundary issue, but the fixed-height contract breaks under browser text zoom / user default font size: at 200% text scaling the 188px panel clips the title and date entirely, and `overflow-hidden` on a fixed box makes the content unreachable rather than scrollable. Fix: express the vertical contract in `rem`-relative units or `min-h` so the panel grows with text, and include one zoomed screenshot in the evidence set.

MINOR architecture-and-reuse "Architecture Improvement Opportunities #2" — two component-local hex values are accepted with no statement about theme context; `docs/systems/coding-conventions.md` documents `sidebar-*` dark tokens, so a hardcoded light-gray panel and neutral outline may render as light-on-light or invisible if the dashboard surface has a dark variant. Fix: confirm whether `/projects` renders under a dark surface (needs `src/app/globals.css` and the dashboard shell) and, if so, define the two colors as tokens with both variants instead of raw hex.

MINOR architecture-and-reuse "Architecture Improvement Opportunities #1" — the plan introduces a hand-rolled card container without addressing the existing shadcn/CVA `Card` primitive that the coding conventions doc implies exists. If `src/components/ui/card.tsx` is present, this creates a second, divergent card surface whose border/radius drift from the rest of the app on the next token change. Fix: state explicitly whether the redesigned card composes the existing `Card` primitive or deliberately bypasses it, with the reason.

MINOR verification "Plan" checklist item 3 — "Update system documentation describing the card layout" names no file, so the self-healing docs rule has no verifiable target and the step can be marked done with nothing changed. Fix: name the doc (likely `docs/systems/directories-and-key-files.md` or the architecture doc's shared-UI section) and the specific lines to update.

MINOR product-and-scope "Critique — Customer Or End User" — "one-line title" is a silent behavior reduction: project names that share a prefix become visually identical in the grid, and the plan provides no `title` attribute or tooltip fallback. Fix: add a native `title` on the truncated name so the full project name stays reachable on hover and to assistive tech.
