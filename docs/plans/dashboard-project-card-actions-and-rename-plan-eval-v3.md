BLOCKER Security and data integrity — Test Strategy / Assumptions (Playwright rename-and-restore on a retained real project) — the restore path writes the captured original name back through the *newly hardened* PATCH, which NFKC-normalizes, collapses whitespace, and strips control/bidi/zero-width characters; if the retained project's original name contains any of those (double spaces, a trailing NBSP, an em-dash-adjacent ZWSP, a full-width character), the "restored" value is not the original and the mutation is silent and permanent. Every test run permanently rewrites a real user-visible project name with no diff or alert. Fix: capture the original `name` before the first rename, and after restore assert the reloaded value byte-equals the captured original; if the captured original is not normalization-stable, fail fast and skip the mutation test rather than write a lossy restore.

BLOCKER Verification and rollback — Test Strategy (`finally` cleanup retries "the authenticated owner-scoped PATCH restoration") — the plan never says how the out-of-page cleanup obtains an authenticated session; a raw `fetch` from the Node test process has no Supabase cookie, so the retry returns 401 and the "hard failure" fires *after* the project is already left renamed to an 80-character marker. Any crash, timeout, or CI cancellation between rename and restore strands a real project under a garbage name with no automated recovery. Fix: perform restore through the same authenticated browser context (page-level `request` fixture carrying storage state), and if that context is gone, fail the run with an explicit "MANUAL RESTORE REQUIRED: project <id> original name <captured>" message.

MAJOR Security and data integrity — Boundary And Cache Semantics (name normalization) — stripping "zero-width controls" removes U+200C ZWNJ and U+200D ZWJ, which are semantically required in Persian, Hindi, and other scripts and in emoji ZWJ sequences. A user whose project is named in Farsi has the name silently corrupted on their next rename, and a family-emoji name collapses into unrelated glyphs. Fix: strip only C0/C1, U+200B/U+FEFF, and the bidi override/embedding/isolate set (U+202A-202E, U+2066-2069); explicitly preserve U+200C/U+200D and document that choice in the validator test.

MAJOR Runtime and change impact — Boundary And Cache Semantics + Assumptions (80 UTF-16 code-unit cap on both clients) — NFKC normalization can *expand* length (ﬁ→fi, ㍿→株式会社), so a client `maxLength={80}` applied to raw input can yield a post-normalization value over 80, and a client-side "cap" that truncates at 80 code units can split a surrogate pair or a combining sequence, producing a lone surrogate. The user sees a generic inline error for input the UI accepted, or a mojibake name persisted. Fix: specify normalize-then-measure order everywhere, never truncate on the client (reject with a specific "80 characters maximum" message), and add validator tests for an NFKC-expanding input and an astral-plane character at the boundary.

MAJOR Product and scope — Plan / Test Strategy (legacy rows exceeding the manual rule) — the Critique explicitly accepts that legacy rows may exceed 80 code units or contain characters the new rule strips, but the rename modal prefills from the existing name; a user opening Rename on such a project gets a pre-filled value that fails validation or is silently altered, with only a generic inline error and no way to keep their current name. Renaming becomes a dead end on exactly the projects most likely to need it. Fix: state the prefill rule — prefill with the normalized value and, when it differs from the stored name, either allow save of the normalized form or show a specific message; add a validator/UI case for an over-length stored name.

MAJOR Verification and rollback — Risk-Matched Verification ("Save failure or duplicate write" → "Code-path review and disabled-state assertions") — the failure path, duplicate-submit guard, and response-derived display/href update have no automated test that fails when they break; code review is not a regression net, and the single Playwright journey only exercises the success path. A regression that re-enables Save mid-flight (double PATCH) or clears the modal on a 400 ships green. Fix: extract the rename submit into a pure function taking an injected fetch and returning next-state, and unit test 400/500/network-error, in-flight rejection of a second submit, and normalized-response-derived name/href.

MAJOR Runtime and change impact — Ownership, Scope, And Lifetime ("Draft, saving, error, display-name, and canonical href state ... reset on close or a changed server prop") — resetting the *draft* on a changed server prop means any `router.refresh()` (triggered by a rename on a different card, or any other dashboard revalidation) wipes text the user is mid-way through typing in an open modal. User loses in-flight input with no warning. Fix: sync only display name and href from props; never touch draft/saving/error while the dialog is open.

MAJOR Verification and rollback — Test Strategy (PATCH hardening) — coverage for the route is described as "focused validator tests and route use"; there is no test that the route itself returns 400 for non-string/blank/over-length bodies, nor that auth and the `user_id` filter still apply after the rewrite. If the validator is imported but only wired into one branch, or the ownership filter is dropped during the rewrite, nothing fails. Fix: add a route-handler test (Node runner, mocked Supabase client) asserting 401 unauthenticated, 404/403 for a non-owned id, 400 for each invalid body shape, and 200 returning the normalized name.

MAJOR Runtime and change impact — Test Strategy (touch/mobile) — the plan claims the menu makes actions "discoverable on touch" but verifies only desktop and narrow *viewport widths* in Chrome; a narrow width is not a touch context. A pointer-enabled trigger overlaid on a full-card anchor is exactly where touch tap-through / synthesized-click navigation regressions occur, and the shipped behavior would be tapping the menu and landing in the workspace. Fix: add one Playwright project or context with `hasTouch: true` and assert `tap()` on the trigger opens the menu and leaves the URL at `/projects`.

MINOR Architecture and reuse — Ownership, Scope, And Lifetime (`src/lib/project-name.ts` shared by `project-name-generation.ts`) — routing generated-name sanitation through the new normalizer changes AI naming output beyond the stated goal; if generation currently permits characters the manual rule strips, intake-created project names change and any existing naming assertions move. Fix: either scope the shared module to the 80-code-unit constant plus the manual validator and leave generation sanitation untouched, or add an explicit step verifying generated-name behavior is unchanged, with the existing naming tests named.

MINOR Premises — Boundary And Cache Semantics (`getProjectUrl()` / `buildProjectRef()` described as "client-safe") — I cannot confirm this module is free of server-only imports or `next/headers`; if it is not, importing it into `dashboard-project-card.tsx` breaks the client build or drags server code into the bundle. Fix: name the exact file in the plan and add a step asserting it has no server-only imports; I would need to read the module defining `getProjectUrl`/`buildProjectRef` (referenced by the workspace canonicalizer) to close this.

MINOR Runtime and change impact — Runtime And Change-Impact Analysis ("The projects dashboard does not render generation polling/progress state") — this premise carries the whole `router.refresh()` safety argument but is asserted, not evidenced; the dashboard cards display mockup metadata and the repo has `generate-all-store.ts` plus `/api/projects/[id]/onboarding-status`. Fix: cite the file and line proving no polling/progress client mounts under `/projects`; I would need `src/app/(dashboard)/projects/page.tsx` and `dashboard-project-card.tsx` to verify.

MINOR Security and data integrity — Plan (PATCH hardening) — no optimistic-concurrency or `updated_at` check is mentioned, so a rename from the dashboard silently overwrites a concurrent rename from an open workspace header in another tab, and the losing tab's prop-sync effect will show the winner's name with no indication a write was lost. Fix: acknowledge last-write-wins explicitly in the plan, or return and compare `updated_at` and surface a "changed elsewhere" inline message.

MINOR Verification and rollback — Test Strategy ("a second card's action opens") — the journey requires at least two projects on the retained E2E account, which is an unstated precondition; with one project the assertion fails as a false regression. Fix: assert the precondition explicitly with a clear skip/fail message naming the required fixture state.

MINOR Product and scope — Assumptions (overflow menu on every populated card) — the plan never states whether Rename is entitlement-gated; Delete is gated to paid with an upgrade prompt, so a reviewer could reasonably implement Rename the same way. Fix: state explicitly that Rename is available to all tiers including Free and that no allowance check is added.
