BLOCKER runtime-and-change-impact — "Decision 3 / Failure And Recovery / Rollback" — style choice is not persisted, yet the plan claims retry determinism from "project ID, MVP plan, primary platform, direction"; the MVP plan is user-editable and regenerable, so a single-option regeneration after any MVP edit recomputes a different triad while options A and C were already rendered and paid for. User pays for an option that no longer matches its siblings, and with no persisted treatment ID there is no way to reproduce or debug the original. Fix: persist the three treatment IDs plus catalog version in the existing mockups metadata JSON (the "Deferred" persistence row) and have retry read them instead of re-deriving.

BLOCKER architecture-and-reuse — "Runtime selector / Decision 2" — Decision 2 already discards raw BM25 ordering in favor of hard filters, category-distinctness, and three curated roles, so the ported ranking barely determines the output, yet Phases 1–2 spend the bulk of the work on a BM25 port, a CSV vendoring pipeline, a generator script, and upstream parity fixtures. You build and maintain a search engine whose ranking is overridden downstream, plus a parity test that constrains nothing observable. Fix: generate a build-time lookup from (product category × platform × direction role) to a curated triad, check in that small table, and delete the runtime ranking, the runtime catalog, and the parity fixture phase.

MAJOR verification-and-rollback — "Risk-Matched Verification, row 1" — golden fixtures are to be "produced by pinned upstream v2.14.1 search," but no phase provisions running upstream Python to emit them, and the acceptance threshold ("parity for fixture queries where the upstream result is accepted") lets any mismatch be reclassified as a rejected candidate. Fixtures will be hand-written from the port's own output, so the test cannot fail for the exact drift it exists to catch. Fix: add an explicit one-time offline step that runs upstream and commits its raw output as a frozen fixture file, and fix the accepted/rejected set before implementing the selector.

MAJOR runtime-and-change-impact — "Repeated Work: catalog import below 2 MB uncompressed" — a multi-megabyte checked-in TypeScript constant imported by an API route is a bundle-size change, and setup-and-build.md records a `guard-webpack-chunky.mjs` build guard plus Vercel serverless packaging. The build can fail or cold starts on the 800s mockup route regress, discovered only at Phase 4. Fix: budget the runtime module at tens of KB by emitting only allowlisted fields for candidate rows, and add the bundle/guard check to Phase 2 acceptance rather than Phase 4.

MAJOR runtime-and-change-impact — "Prompt integration" — the plan sets acceptance thresholds for selector CPU and module size but never for prompt token growth, while injecting palette, typography, effects, density, accessibility caveats, and anti-patterns for three treatments into a planner prompt on a route already documented at a 790s OpenRouter timeout. Longer prompts raise planner latency, cost, and truncation risk on the slowest paid path in the product. Fix: add a hard character/token cap per treatment block and assert total prompt size delta in the Phase 3 snapshot tests.

MAJOR security-and-data-integrity — "Risk, Security, And Operations Critique" — catalog free text is declared "trusted vendored code," but which catalog rows get concatenated into the model prompt is selected by untrusted MVP product text, so user input steers third-party CSV prose into the prompt; directories-and-key-files.md requires `buildSecurePrompt()` with `sanitizeInput()` and `<user_input>` delimiters for interpolated values. Vendored guidance strings (e.g. "Do Not Use For" prose) reach the model unfenced and can override product instructions. Fix: allowlist and length-cap each catalog field, render treatments as structured key/value lines rather than pasted prose, and route any user-derived text through the existing secure-prompt helper.

MAJOR verification-and-rollback — "Phase 4 / Test Strategy: Browser" — verification relies on fixture-mode workspace and built-in image generation, while docs/operating-system/ui-verification.md forbids fixtures, stubs, and bypassing image generation for user-visible changes. The one claim that matters — that the real image model renders three visibly distinct, product-safe treatments — is never tested, and the evidence produced would be rejected at review. Fix: ask the user to opt in to one fresh-project generation-flow evidence run (the fresh-project rule is opt-in, not forbidden) and gate Milestone 4 on it; otherwise state in the plan that the feature ships unverified against its primary provider.

MAJOR runtime-and-change-impact — "Failure And Recovery: zero/unsafe matches" — fallback to the legacy brand bank is scoped "for that triad," but a later single-option retry re-runs selection independently, so one run can mix a Pro Max A/C with a legacy-bank B (or the inverse after a flag flip or deploy), and there is no metric on fallback frequency. Users see an incoherent concept set and the team has no signal that the selector is silently failing. Fix: make fallback a per-run decision recorded with the run (alongside the persisted treatment IDs) so retries inherit it, and emit a content-free counter for fallback rate.

MAJOR premises — "Runtime selector: Candidate filters ... low-accessibility treatments" — the filter rules assume `styles.csv` / `ui-reasoning.csv` carry machine-readable platform-compatibility, contrast/accessibility, and "Do Not Use For" fields; the plan never names the columns. If those signals are prose-only or absent, the entire safety layer degrades to keyword matching on free text and "Experimental" can ship an unusable treatment. Fix: verify the column inventory in the pinned snapshot's `styles.csv` and `ui-reasoning.csv` header rows before Phase 2 and write the filter predicates against named columns in the plan. I could not check this without the vendored CSV headers.

MAJOR verification-and-rollback — "Rollback And Recovery" — flipping `MOCKUP_PRO_MAX_STYLES_ENABLED=0` requires a Vercel env change plus redeploy and does not affect `generation_queue_items` already in flight or partially completed option drafts. Rolling back mid-incident produces projects with one Pro Max option and two legacy options and no way to identify which is which. Fix: read the flag once at run start, persist the resolved mode with the run, and state the in-flight behavior explicitly in the rollback section.

MINOR verification-and-rollback — "Test Strategy: Contract" — asserting that design-plan and API payload snapshots "remain unchanged" passes trivially since no payload change is proposed, and prompt snapshots self-bless whatever the first implementation emits. Neither would fail if the treatment block were semantically wrong. Fix: replace prompt golden blobs with invariant assertions (selected accent hex appears in every primary-CTA instruction; success green appears only in completed-state instructions; three distinct style IDs present).

MINOR premises — "Decision 3 / Runtime selector inputs" — seeding on project ID means two projects with the identical idea get different treatments and a user cannot reproduce a result by recreating a project. Support and QA lose the ability to reproduce a reported visual outcome. Fix: derive the tiebreaker from stable content (idea/platform hash) and use project ID only to break exact ties, or document reproducibility as an accepted loss.

MINOR product-and-scope — "Milestones 1 / Open Decisions: Blocked" — the entire plan is gated on backend approval with no defined behavior if approval is withheld, while Phase 1 (vendoring, provenance, generator) is independent of that approval. Work stalls entirely on a yes/no that need not block the non-prompt phases. Fix: state that Phases 1–2 proceed under approval-independent scope and that only Phase 3 waits, or drop the gate if prompt-string edits already fall inside prior authorization.

MINOR security-and-data-integrity — "Source snapshot / Decision 4" — the plan asserts upstream is MIT and records hashes, but does not verify licensing of the catalog data itself, which may aggregate third-party design-system content under different terms. Vendoring redistributes data the repo may not be licensed to ship. Fix: add a Phase 1 checklist item to confirm the license covering the CSV data files specifically, not just the repository LICENSE, and record the finding in the provenance note.
