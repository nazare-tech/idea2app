BLOCKER premises — Boundary And Cache Semantics / Assumptions — the plan never mentions `/projects/[projectRef]`, and nothing in it establishes whether `projectRef` is an opaque id or a slug derived from `projects.name`. If the ref is name-derived, every successful rename silently changes or breaks the workspace URL, invalidates the prefetched warmup target, and 404s existing bookmarks/links. Fix: read `src/app/projects/[projectRef]/page.tsx` and the card's href construction, state the finding explicitly in Assumptions, and if the ref is name-derived add a phase covering ref regeneration or pinning.

BLOCKER verification — Test Strategy / Risk-Matched Verification — the only evidence for a successful rename is "code-path review and disabled-state assertions"; every browser and Playwright step explicitly cancels without saving, so no test at any layer exercises PATCH success → DB write → card display update → `router.refresh()`. A rename that 200s but writes nothing, writes the untrimmed value, or leaves the card showing the old name ships green. Fix: add one e2e that renames a retained project to a marker value, asserts the persisted card text after refresh, then renames it back — this is a mutation-and-restore, not the prohibited create/generate.

MAJOR premises — "preserving ... existing workspace rename compatibility" — the plan asserts, without evidence, that the shared 80-char/trim/string-only validator is compatible with every current caller of `PATCH /api/projects/[id]`. If the naming pipeline or workspace header ever writes a generated name longer than 80 chars or a non-`{ name }` body, those calls start returning 400 after this change. Fix: enumerate callers of the route (grep `api/projects/${` and the naming lib) and the actual generated-name cap before fixing the limit; if generated names are unbounded, apply the cap only to the manual-rename path.

MAJOR runtime — Ownership, Scope, And Lifetime — "a rename updates only the owned `projects.name` row and the affected card" ignores every other surface that already holds the name: the workspace `ProjectHeader`, `generate-all-store.ts`, and any cached RSC payload for `/projects/[projectRef]`. A user renames from the dashboard, navigates into the project, and sees the old name until a hard reload. Fix: add a step to invalidate/refresh the workspace route (e.g. `revalidatePath` on the project route from the PATCH handler, or document the staleness as accepted with a named reason).

MAJOR runtime — Repeated Work ("No polling, timers ... is added") — `router.refresh()` re-renders the whole dashboard RSC tree, including cards for projects with in-flight Generate All / onboarding-status polling. Renaming during an active generation can remount those cards and reset their client-side progress/streaming state, making a generation look stalled. Fix: verify what the dashboard renders for `status = generating`, and prefer local state update plus a scoped revalidation over a blanket `router.refresh()` — or verify explicitly under an active generation.

MAJOR change impact — Plan step 2 ("remove the media hover trash button") — no step audits existing Playwright specs, unit tests, or docs that select the hover trash control or its confirm modal wiring. Existing delete coverage breaks or, worse, silently passes on a selector that no longer resolves, leaving delete unverified while the plan claims "Delete regression: existing free smoke path". Fix: add a step to grep the test suite and `docs/systems/` test-inventory for the trash-button selector and repoint every hit at the menu item before deleting the control.

MAJOR verification — Risk-Matched Verification, "Title/menu collision" row — the acceptance threshold ("title truncates before reserved action slot") is checked by a server-rendered contract test with no DOM/layout engine plus screenshots of whatever names the retained test projects happen to have. A short-named fixture proves nothing about overlap, and the actual collision case ships unverified. Fix: include an explicitly long/no-space name case in the browser bounding-box assertions (title right edge ≤ trigger left edge), not just the static contract test.

MINOR security — Clarifying Question 2 / shared validator — "trim, 1-80 characters, string-only" leaves newlines, control characters, bidi overrides, and zero-width characters accepted, and does not define whether 80 counts UTF-16 code units, code points, or graphemes. A name containing `\u202E` or newlines renders as a broken or spoofed title in the card, workspace header, and document title. Fix: specify collapse-whitespace, strip C0/C1 and bidi control chars, Unicode-normalize, and state the counting unit in the validator spec and its tests.

MINOR security — Boundary And Cache Semantics — the hardening step covers validation but not rate limiting, even though `rate-limit.ts` already exists and is cited in the architecture doc. An authenticated client can drive unbounded writes to `projects.name`. Fix: apply the existing rate limiter to the PATCH route in the same step, or state why this route is exempt.

MINOR product — Assumptions ("Delete remains visible in the menu for Free users but opens the existing upgrade prompt") — promoting Delete from a hover-only affordance to an always-visible menu item multiplies how often free users select a destructive action that only yields an upsell. Fix: render the Free-tier item as disabled with an inline reason, or keep it enabled but confirm with the user that surfacing the upgrade prompt more often is the intended product behavior.

MINOR verification — Test Strategy — the evidence path `ui-evidence/2026-08-09/project-card-actions-rename/` hardcodes a date that will be wrong if implementation happens on any other day, and the plan gives no rule for which date wins. Fix: specify `ui-evidence/<implementation-date>/project-card-actions-rename/`.
