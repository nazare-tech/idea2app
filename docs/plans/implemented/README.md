# Implemented Plans Archive

This folder keeps historical implementation plans and review notes after the work has shipped.

When an implemented plan conflicts with current behavior, use `PROJECT_CONTEXT.md` and the current source code as the source of truth. Historical plans may describe an intermediate implementation that was later refined by another shipped change.

Known superseded detail:
- `mockup-screen-count-limits-plan.md` describes an older 1-2 desktop / 1-3 mobile screen-count contract. The current mockup storyboard path uses fixed two-frame skeletons and exactly two planned screens for every platform.
